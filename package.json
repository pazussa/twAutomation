{
  "name": "autoloop-twilio-playwright",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "playwright:install": "playwright install",
    "pw": "playwright test",
    "pw:select": "playwright test",
    "pw:report": "playwright show-report",
    "report:pdf": "node scripts/export-report-to-pdf.mjs",
    "admin": "node --loader ts-node/esm src/admin/server.ts",
    "admin:start": "./start-admin.sh",
    "sync": "node scripts/sync-yml-to-data.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.55.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}